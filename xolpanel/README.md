```
apt update && apt install wget -y && wget https://raw.githubusercontent.com/xnnnsets/XRAY-MANTAP/master/xolpanel.zip && unzip xolpanel.zip && cd xolpanel && chmod +x xolpanel.sh && ./xolpanel.sh
```

**Bentar belom bisa**
